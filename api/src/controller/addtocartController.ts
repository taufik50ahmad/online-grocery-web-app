import type { Request, Response } from "express";

export default function addtocartController(req: Request, res: Response){
    try {
        
    } catch (error) {
        console.log(error)
    }
}